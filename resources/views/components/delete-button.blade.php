<button data-delete-route="{{ $route }}" title="{{ __('Delete') }}"
    class="flex items-center justify-center bg-red-500 hover:bg-red-600 text-white p-2 rounded-lg transition-colors duration-200">
    <i class="fas fa-trash"></i>
</button>
